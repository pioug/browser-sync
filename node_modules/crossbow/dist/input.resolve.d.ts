import { CrossbowInput } from "./index";
import { CrossbowConfiguration } from "./config";
import * as file from "./file.utils";
export declare enum InputTypes {
    DefaultExternalFile,
    ExternalFile,
    InlineObject,
    StubInlineObject,
    CBFile,
    InlineJSON
}
export interface UserInput {
    errors: any[];
    sources: file.ExternalFileInput[];
    type: InputTypes;
    inputs: CrossbowInput[];
}
export declare function getInputs(config: CrossbowConfiguration, inlineInput?: any): UserInput;
/**
 * `Input` is the object that is looked at to resolve tasks/options and
 * watchers
 */
export declare function generateBaseInput(incoming: CrossbowInput | any): CrossbowInput;
